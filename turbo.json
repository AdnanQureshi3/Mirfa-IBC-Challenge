{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["DATABASE_URL", "MASTER_KEY"],
  "tasks": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"],
      "env": ["DATABASE_URL", "MASTER_KEY"]
    }
  }
}